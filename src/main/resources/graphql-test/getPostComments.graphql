query getPostComments($postId: ID!) {
    getPostComments(postId: $postId) {
        id
        text
        authorUser {
            id
            name
        }
        post {
            id
            text
            authorUser {
                id
                name
            }
        }
    }
}