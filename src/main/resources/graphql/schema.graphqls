type Query {
    getUsers : [UserEntity]
    getUser(id: ID!) : UserEntity

    getPosts: [Post]
    getPost(id: ID!) : Post

    getComments: [Comment]
    getComment(id: ID!) : Comment
}

type UserEntity {
    id: ID!
    name: String!
}

type Post {
    id: ID!
    authorUserId: Int!
    text: String!
}

type Comment {
    id: ID!
    postId: Int!
    authorUserId: Int!
    authorUser: UserEntity!
    text: String!
}